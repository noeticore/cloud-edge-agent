"""Benchmark — privacy detection and sanitization evaluation."""

from pathlib import Path

BENCHMARK_DIR = Path(__file__).resolve().parent
DATA_DIR = BENCHMARK_DIR / "data"
RESULTS_DIR = BENCHMARK_DIR / "results"
